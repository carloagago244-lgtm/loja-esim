# loja-esimimport { useState } from "react";
import { Card, CardContent } from "@/components/ui/card";
import { Button } from "@/components/ui/button";

// Loja eSIM – Tema Roxo + PIX + Descrição completa
export default function LojaEsim() {
  const [finalizar, setFinalizar] = useState(false);

  const produto = {
    nome: "eSIM Vivo 46GB",
    preco: 10,
    descricao:
      "O eSIM Vivo 46GB oferece internet rápida e estável, ideal para redes sociais, vídeos, chamadas e uso diário. A ativação é feita via QR Code, liberada logo após o pagamento. Compatível com aparelhos que aceitam eSIM.",
  };

  const whatsappLink = `https://wa.me/81995730422`;

  return (
    <div className="min-h-screen bg-purple-100 p-6 flex flex-col items-center">
      <h1 className="text-3xl font-bold mb-6 text-purple-800">Loja Oficial de eSIM</h1>

      <Card className="w-full max-w-md shadow-xl rounded-2xl border-purple-300">
        <CardContent className="p-6 flex flex-col gap-4">
          <h2 className="text-xl font-semibold text-purple-900">{produto.nome}</h2>
          <p className="text-gray-700">{produto.descricao}</p>
          <p className="text-3xl font-bold text-purple-700">R$ {produto.preco},00</p>

          <Button
            className="w-full py-3 text-lg rounded-xl bg-purple-700 hover:bg-purple-800"
            onClick={() => setFinalizar(true)}
          >
            Finalizar Pedido
          </Button>
        </CardContent>
      </Card>

      {finalizar && (
        <div className="mt-8 bg-white shadow-lg p-6 rounded-2xl w-full max-w-md text-center">
          <h3 className="text-xl font-bold text-purple-800 mb-4">Pagamento via PIX</h3>

          <p className="text-gray-700 mb-2">Use a chave abaixo para realizar o pagamento:</p>

          <div className="bg-purple-100 border border-purple-300 p-4 rounded-xl mb-4">
            <p className="font-semibold text-purple-900">Chave PIX (Telefone):</p>
            <p className="text-lg font-bold text-purple-700">81995730422</p>
            <p className="mt-2 text-sm text-purple-800">Titular: Carlos Henrique da Silva</p>
          </div>

          <p className="text-gray-600 mb-4">
            Após realizar o pagamento, envie o comprovante pelo WhatsApp para receber seu eSIM imediatamente.
          </p>

          <a
            href={whatsappLink}
            target="_blank"
            className="block w-full py-3 bg-purple-700 text-white rounded-xl text-lg mt-2"
          >
            Enviar comprovante pelo WhatsApp
          </a>
        </div>
      )}
    </div>
  );
}
